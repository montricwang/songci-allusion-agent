v0.2.0-alpha 目的是增加对全宋词的检索支持

全宋词有部分繁体字，需要使用 OpenCC 进行繁转简

chinese-poetry 中的全宋词是按句号切分，而不是按照更细粒度切分。embedding 时要考虑是按更细粒度切分还是句号切分，或者选择折中的方式。
目前考虑直接按现有格式（句号）进行 embedding，观察效果
之后如果多种切分力度混合，可尝试采用原子化索引 + 动态上下文展示策略。检索时按单句确保高精度，展示时通过 parent_id 和 line_idx 聚合去重并扩展窗口，避免向量语义重叠导致的冗余展示。